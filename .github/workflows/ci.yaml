name: ci

on:
  pull_request:
  push:
    branches:
      - "main"

jobs:
  lint-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo fmt --check
      - run: cargo check
      - run: cargo clippy
      - run: cargo test
